/**
   Função que recebe dois números inteiros e retorna a soma deles.

   - Parameter number1: O primeiro número inteiro a ser somado.
   - Parameter number2: O segundo número inteiro a ser somado.
   - Returns: A soma dos dois números fornecidos.
   - Note: Esta função calcula a soma dos dois números fornecidos e imprime o resultado na saída padrão.

   - Example:
     ```
     let result = sum(number1: 5, number2: 3)
     // Resultado: amount: 8
     ```

   - Author: Seu nome aqui
   - Version: 1.0
*/
func sum(number1: Int, number2: Int) {

    let value = number1 + number2

    print("amount: \(value)")

}


sum(number1: 10, number2: 10)
